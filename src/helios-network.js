/**
 * Primary entry that re-exports the Helios network API.
 * Consumers can import directly from `helios-network`.
 */
export { default, AttributeType, CategorySortOrder, DenseColorEncodingFormat, DimensionDifferenceMethod, getHeliosModule, NodeSelector, EdgeSelector } from './js/HeliosNetwork.js';
